<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Weather App</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
    <div class="container">
        <h3 class="card-panel teal white-text center-align">OpenWeather</h3>
        <span class="hspace1"></span>
        <div class="row">
            <form id="WeatherForm" name="WeatherForm" class="col s12" method="POST" action="/">
                <div class="row">
                    <div class="input-field col s12 m6">
                        <input id="city" name="city" type="text" class="validate">
                        <label class="active" for="city">City</label>
                    </div>
                    <div class="input-field col s12 m6">
                        <input id="country" name="country" type="text" class="validate">
                        <label class="active" for="country">Country</label>
                    </div>
                    <div class="col s12">
                        <span class="hspace1"></span>
                        <button form="WeatherForm" class="btn waves-effect" type="submit">
                            <i class="material-icons left">cloud</i>Summit</button>
                    </div>
                </div>
            </form>
        </div>
        <% if(querySuccess){ %>
        <div class="row">
            <div class="col s12">
                <h4>The temperature in <%=city%> (<%=countryCode%>) is <%=temperature.celsius%>&deg;C or
                    <%=temperature.fahrenheit%>&deg;F</h4>
            </div>
        </div>
        <% } %>
        <% if(!querySuccess && requestSuccess){ %>
        <div class="row">
            <div class="col s12 red-text text-darken-4">
                <h4>Error, try again</h4>
            </div>
        </div>
        <% } %>

    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html>